# Schema Reference

## model

The `model` node holds all databinding state (instances) and logic (rules).

### instance

Each `instance` 

### rules

## pages

### page (implicit)

#### field (implicit)

- `type`
- 

